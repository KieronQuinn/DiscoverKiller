<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kieronquinn.app.discoverkiller">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Debuggable required for overlay -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".DiscoverKiller"
        android:theme="@style/AppTheme.Splash"
        android:debuggable="true"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".ui.activities.SettingsActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.SettingsBackgroundPickerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>

        <meta-data
            android:name="xposedmodule"
            android:value="true"/>
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_desc"/>
        <meta-data
            android:name="xposedminversion"
            android:value="90"/>
        <meta-data
            android:name="xposedscope"
            android:resource="@array/scope"/>

        <provider
            android:name=".components.settings.RemoteSettingsProvider"
            android:authorities="com.kieronquinn.app.discoverkiller.provider.remotesettings"
            android:grantUriPermissions="true"
            android:exported="true" />

        <activity
            android:name="com.google.android.gms.oss.licenses.OssLicensesMenuActivity"
            android:theme="@style/AppTheme.ThirdPartyLicences" />

        <activity
            android:name="com.google.android.gms.oss.licenses.OssLicensesActivity"
            android:theme="@style/AppTheme.ThirdPartyLicences" />


    </application>

</manifest>